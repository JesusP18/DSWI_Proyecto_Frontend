@model IEnumerable<DSWI_Proyecto_Frontend.Models.Complejidad>

@{
    ViewData["Title"] = "ListarComplejidad";
}

<div class="container">
    <h1>Lista de Complejidades</h1>

    <p>
        <a class="btn btn-success" asp-action="CrearComplejidad">Crear Nueva Complejidad</a>
    </p>
    <div class="container card" style="box-shadow: 5px 5px 20px rgba(0,0,0,0.4)">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>
                        ID
                    </th>
                    <th>
                        Descripción
                    </th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.IdComplejidad)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Descripcion)
                        </td>
                        <td>
                            @Html.ActionLink("Editar", "EditarComplejidad", new {  id=item.IdComplejidad}, new {@class = "btn btn-warning" })
                            @*@Html.ActionLink("Details", "Details", new { id=item.IdTarea})*@
                            @Html.ActionLink("Eliminar", "EliminarComplejidad", new {  id=item.IdComplejidad  }, new {@class = "btn btn-danger" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
